{
  "name": "pedicalc",
  "version": "1.0.0",
  "description": "PediCalc — a pediatric drug dose calculator (offline, vanilla ES-module web app).",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node build/bundle.mjs",
    "test": "node --test",
    "smoke": "node tests/boot.smoke.mjs",
    "smoke:bundle": "node build/bundle.mjs && node tests/bundle.smoke.mjs"
  },
  "license": "MIT"
}
